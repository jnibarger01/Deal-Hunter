# Memory — 2026-03-02

## Projects Built / Shipped Today

### smart-organizer (Rust CLI)
- Built in `/home/jace/.openclaw/workspace/smart-organizer`
- Deterministic rule engine, pattern tagging, optional embedding clustering, dry-run, audit-mode, undo journal (JSONL + rollback)
- `cargo test` and `cargo build --release` passing
- Binary at `target/release/smart-organizer` (must invoke directly or `cargo install --path .` first)

### ops-automation
- Location: `/home/jace/.openclaw/workspace/ops-automation`
- #11 Daily Brief: sends to Telegram at 7:00 AM via systemd user timer (`daily-brief.timer`)
- #3 Urgent Email Scan: runs every 30 min during waking hours (weekday 17-21, weekend 7-21) via `urgent-email-scan.timer`
- Telegram target: `8593378188`; channel flag: `--channel telegram` required
- `FORCE_RUN=1` env var overrides time-gate for manual testing
- `DRY_RUN=0` set in config/.env for live sends
- Daily brief successfully sent (live, Message ID 1463)
- SQLite state at `state/automation.db`; feedback via `urgent_feedback.sh`

### git-auto-sync
- Location: `/home/jace/.openclaw/workspace/git-auto-sync`
- systemd user timer hourly; creates timestamp tags `auto-sync-YYYYMMDD-HHMMSS`
- Remote: `https://github.com/jnibarger01/deal-hunter.git` (case: Deal-Hunter)
- `.env` uses `export KEY=VAL` syntax → systemd ignores; script sources it via `source` in ExecStart (works)
- Last confirmed success: `auto-sync-20260302-120117`; push confirmed working after GitHub SSH re-auth

### db-backup
- Location: `/home/jace/.openclaw/workspace/db-backup`
- AES-256 encrypted backup → Google Drive; retain last 7 slots
- BLOCKED: `gog` Drive auth still failing (`failed to authenticate`)
- Restore script tested and confirmed working (local round-trip)

### Toyota Maintenance Scraper
- Location: `/home/jace/Toyota-maintenance-scraper`
- 4/4 tests passing (fixed import path issue)
- CI via `.github/workflows/ci.yml`, `Makefile` added by Claude Code agent
- `go_no_go.sh` created and executable at repo root
- `toyota-scraper.timer` scheduled daily at 06:30 (systemd user)
- Commits: `4699505`, `372c4a3`

### JCC (Jace's Command Center)
- Location: `/home/jace/JCC`
- Operating system defined in `CommandCenter/OPERATING_SYSTEM.md`
- Daily cadence: 15 min AM + 10 min PM
- Queue (`queue.md`), routing log, scoreboard, daily/weekly runbook templates all upgraded

## System / Infrastructure

### Tailscale
- Installed and active on Fedora
- Tailscale IP: `100.70.28.73`; device: `fedora.tailaa6d41.ts.net`
- iPhone also on tailnet: `100.120.217.100`
- SSH enabled via `sudo tailscale up --ssh`
- ACL policy created (tag:server for Fedora, only jnibarger01@gmail.com has access)

### Sleep/Lock Timers
- Sleep (AC + battery): 1 hour (3600s)
- Screen idle: 20 min (1200s)
- KDE/SDDM lock timeout: 60 min via `kwriteconfig5`

### GitHub Auth
- SSH key generated and uploaded (`claw` key)
- Re-authed via `gh auth login -h github.com` (SSH mode)
- Device code: 8180-2664 (used one time)
- Remote: `https://github.com/jnibarger01/deal-hunter.git` → `Deal-Hunter`

### gog CLI
- Installed at `/home/jace/.cargo/bin/gog` (version 0.1.0)
- Symlinked to `/home/jace/.local/bin/gog` for PATH access
- Gmail auth working (via OAuth OAuth client `167704805236-ngf9vnhlo22cqnundpqpists2b03gnhq`)
- Drive auth NOT working (`failed to authenticate`)
- `--services` flag doesn't support all services; use `gog auth add email@gmail.com` (no --services flag)
- OAuth client secret at: `/home/jace/.config/gogcli/credentials.json`
- Original download: `/home/jace/Downloads/client_secret_167704805236-...json`
- Multiple channels configured → must always pass `--channel telegram` in openclaw message sends

### OpenClaw
- Update available: `npm 2026.3.1`
- CRITICAL: `plugins.allow` not set; msteams plugin loading without provenance
- Tailscale shows as "off" in `openclaw status` (but Tailscale IS running separately)
- WhatsApp connected at +13163032715

### System Crash (Mar 1 ~21:01)
- `node-22` dumped core in V8 compiler (`LiveRangeConnector::ResolveControlFlow`)
- Chrome also logged bad page state immediately after
- Likely cause: Node.js + V8 JIT OOM or race during heavy compilation load
- ABRT logged it; no data loss observed

## Key Decisions / Preferences
- Project name choice: **BrewHub** over BeanHub or coffee-shop
- Jace's Gmail: `jnibarger01@gmail.com`
- Telegram chat ID: `8593378188`
- Workspace: `/home/jace/.openclaw/workspace`
- Toyota scraper project: `/home/jace/Toyota-maintenance-scraper`
- GitHub: `github.com/jnibarger01` (Deal-Hunter repo)

## Pending / Blockers
- `gog` Drive auth needs to be resolved before backup-to-Drive works
- `DRIVE_FOLDER_ID` for backup not yet set (used `root` as placeholder)
- Daily brief calendar/contact section shows empty (gog calendar auth needed)
- msteams duplicate plugin warning needs `plugins.allow` set in OpenClaw config
- Toyota scraper `make run` (live network) takes ~15+ min; don't run interactively without intent

## Appointments (Jace's workbook — 2026-03-02)
- Total today: 102
- In-dealership (D channel): 21
- Avg vehicle mileage: ~67,143 mi
- Jace's 5 appointments: Patricia Schongar (7AM), Nicholas Moncrief (7:40AM), Kelsea Chandler (8AM), Mark Eisenbraun (2PM), Harold Reed (3:20PM)
